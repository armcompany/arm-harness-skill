interface:
  display_name: "Harness Engineering"
  short_description: "Build persistent, stack-aware agent workflows"
  default_prompt: "Use $harness-engineering to detect this repository’s stack and create or resume a persistent, project-owned workflow through verified completion."
